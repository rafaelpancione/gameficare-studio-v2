{
    "name": "gameficare-studio",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "npm --prefix client start",
        "build": "npm --prefix client build",
        "lint": "npm --prefix client run lint"
    },
    "devDependencies": {
        "eslint": "^8.57.1"
    }
}
